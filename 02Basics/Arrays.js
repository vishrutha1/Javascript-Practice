const arr = ['Madam', 1 , true]

// arrays starts with zero index like arr[0]

console.log(arr)
console.log(arr[1])

//length of arrays 
console.log(arr.length)

//last but one arr variable
console.log(arr[arr.length-1])

//To get javascript code in pure string using jquery
//`` - Tilda
console.log(`we have ${arr.length} values in specified array`)